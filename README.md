# TransmissionDelay
Grid model using direct current optimal power flow (DC OPF) for transmission expansion planning. Transmission delays are included in the decision of an optimal line.

Transmission expansion modeled with binary variables of constructing new lines. Objective in expansion is to minimize curtailment in the case of abnormally high demand.
